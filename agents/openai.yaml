interface:
  display_name: "Image Studio"
  short_description: "Generate/edit images via OpenAI or Running Hub"
  default_prompt: "Use $image-studio to generate or edit an image through OpenAI-compatible or Running Hub APIs, then return the saved image, metadata, and raw response paths."
